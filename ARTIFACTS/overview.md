# KnowledgeForge 2026-06-14 下午更新摘要

## 完成内容

### 1. 启动脚本整理
- 8个脚本 → 1个 `run.bat`
- 增强：自动检测并启动 Qdrant

### 2. 清空知识库功能
- `kb_query.py` 新增 `clear_collection()` / `delete_collection()` / `list_collections()`
- UI 知识库管理页面支持清空/删除/新建（含二次确认）

### 3. 知识库路径选择
- 侧边栏新增集合下拉选择器
- 设置页新增知识库文件根目录配置（持久化到 .env）
- 摄入管理页显示当前集合和路径

### 4. 知识分类方案
- kb_query 底层原生支持 collection 分类 — 只是之前 UI 没暴露
- 推荐三套方案（见下方详情）

## 修改文件
| 文件 | 变更 |
|------|------|
| `run.bat` | 重命名+增强（Qdrant自启） |
| `kb_query.py` | +150行：4个新函数 |
| `app.py` | +180行：集合选择器、知识库管理翻新、路径配置 |
| 删除 7 个旧脚本 | 清理 |

## 测试建议
1. 双击 `run.bat` 启动 Web UI
2. 到「知识库管理」→「操作」新建几个集合（如 `mechanical`, `materials`）
3. 侧边栏切换到不同集合，分别摄入测试文件
4. 问答页面验证每个集合独立检索
5. 测试清空/删除功能
